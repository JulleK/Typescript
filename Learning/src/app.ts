import _ from "lodash";

// ---- array ----
// const arr = [1, 2, 3, "a", "b", "c", "hello", "world", true, false, "", 0];
// const rand = Math.floor(Math.random() * 5) + 1;

// const shuffled = _.shuffle(arr);
// const chunked = _.chunk(shuffled, rand);

// console.log(chunked);
// console.log(_.last(chunked));

// ---- date ----
// console.log(_.now());
